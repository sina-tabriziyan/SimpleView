package com.sina.spview.models

enum class AnimationState {
    FADE_IN_OUT,
    FADE_OUT_IN,
    TO_LEFT,
    TO_RIGHT
}

enum class BackStackOption {
    CLEAR_CURRENT,
    CLEAR_ALL,
    NO_CLEAR
}